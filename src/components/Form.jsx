import React from "react";

function Form(props) {
  return (
    <div className="form">
      <input type="text" />
      <button>
        <span>Add</span>
      </button>
    </div>
  );
}

export default Form;
